const name = 'conner';
const defaultName = name !== null ? name : 'Default Name';
// can be written as,
const defaultName2 = name ?? 'Default Name'

// xxxxxx short circuit evaluation
const shouldRun = true;

