// Class based components
// functions within the class component
// render -> it returns a jsx element to render the component
// componentDidMount -> on mounting of the component this funciton is run, internally we can run a function to do what ever when it mounts
// componentDidUpdate -> This is done to run some code on any update, it takes in two paramentes which are prevProps, prevState
// componentWillUnmount -> this will run a function when the component is unmounting.

// shouldComponentUpdate -> Takes in params called nextProps, nextState
// This is similar to the React.Memo
// If this function returns true, it will reRender else, it will not reRender
//

// Refs
//
