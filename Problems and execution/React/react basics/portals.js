// Portals
// Stacking context, visit this concept
// It is an internal react HOC function which allow react to render a component outside of the main tree and on top of the present content.
// The component can still take props form the parent component and work as usual.

// createPortal

// ex: function Modal() {
//   return createPortal();
//   reactNode,
//    dom node document.getElementById('modal-root')
// }

//
